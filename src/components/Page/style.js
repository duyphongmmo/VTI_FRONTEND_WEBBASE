const style = (theme) => ({
  root: {
    display: 'flex',
    flexDirection: 'column',
    boxSizing: 'border-box',
    background: theme.palette.bg.main,
    height: '100%',
    overflow: 'hidden',
  },
})

export default style
